#ifndef LABEL_H
#define LABEL_H

const char* OBJECT[] = {
  "Empty",
  "IpadCharger",
  "SSCharger",
  "SSEarpiece",
  "CardReader"
};

#define NUM_OBJECT (sizeof(OBJECT) / sizeof(OBJECT[0]))

#endif
